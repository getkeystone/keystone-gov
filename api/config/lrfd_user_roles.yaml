# THIS FILE IS NOT TRACKED BY GIT (see .gitignore).
#
# Place the real LRFD user role mapping here on the host at:
#   ~/.config/keystone/lrfd_user_roles.yaml
#
# Then either:
#   a) set LRFD_ROLE_CONFIG_PATH=/run/keystone-config/lrfd_user_roles.yaml
#      (file is already mounted from ~/.config/keystone via docker-compose.yml)
#   b) or copy it here if you prefer the config/ volume mount
#
# See api/config/lrfd_user_roles.example.yaml for schema documentation.

version: 1
users: []
